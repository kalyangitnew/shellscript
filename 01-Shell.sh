#!/bin/bash

# #! is called as she-bang.It denotes the path of interpreter and also ensures the script executes with that interpreter.if we execute the script with ./script instead of bash .script.sh

# any line starting with "#" is called as comment and this lines will be ignored during the execution time.
ls
ls -ltr





